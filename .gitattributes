# 一律不做行尾转换。
#
# 这个仓库里有按字节校验的东西：贴图（_verify_bundle.py 直接量 1751018 字节）、
# .moc3、以及 Cubism 要读的 JSON。checkout 时被换成 CRLF 就不是同一份文件了，
# 而"模型文件必须和作者那份逐字节一致"正是 MODEL.md 的立论基础。
# 所以让 git 原样存取，谁在什么系统上 clone 都拿到同样的字节。
* -text
